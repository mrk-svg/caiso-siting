__pycache__/
*.pyc
.DS_Store
.idea/
# raw downloads are re-fetchable; keep the repo small
data/*.xlsx
!data/tpd_*.xlsx
# OASIS raw downloads are re-fetchable (data/poi_pnodes.csv, the hand-confirmed mapping, IS committed)
data/pnodes.csv
data/oasis_cache/
# data/osm_substations.csv is committed on purpose (130 KB, ODbL-attributed): CI needs it to geocode
outputs/projects_all.csv
outputs/cluster15_projects.csv
# snapshots: keep projects.csv (small, the diff needs it), drop the raw xlsx copies
data/snapshots/*/*.xlsx
.venv/
*.egg-info/
.preview/
data/eia860.zip
data/lct_2027.pdf

# Outreach working files: these hold, or will hold, named third parties, their employers and
# the substance of private replies. A public repo is not the place for that, and git history
# would make it permanent.
notes/outreach/targets.csv
notes/outreach/corrections.csv
